<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/btn_image_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="@dimen/margin_15dp"
        android:background="@drawable/background_btns"
        android:contentDescription="@string/content"
        android:padding="@dimen/padding_15dp" />

    <TextView
        android:id="@+id/txt_content_id"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/btn_image_id"
        android:layout_alignStart="@id/btn_image_id"
        android:layout_alignLeft="@id/btn_image_id"
        android:layout_alignEnd="@id/btn_image_id"
        android:layout_alignRight="@id/btn_image_id"
        android:layout_marginTop="@dimen/margin_5dp"
        android:maxLines="2"
        android:textAlignment="center"
        android:textColor="@color/black_color"
        android:textSize="16sp" />

</RelativeLayout>